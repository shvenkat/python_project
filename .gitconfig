[core]
    whitespace = blank-at-eol,blank-at-eof,space-before-tab,-indent-with-non-tab,tabwidth=4
    eol = native
    ; autocrlf is not set here so that the user's global setting (in ~/.gitconfig) can take effect.

[push]
    default = simple
